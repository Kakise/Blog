<% if (post.date) { %>
    <div class="<%= class_name %>">
        <i class="icon-calendar"></i> <time datetime="<%= date_xml(post.date) %>" itemprop="datePublished"><%= date(post.date, config.date_format) %></time>
    </div>
<% } %>
